<h1><%= t "conemo.views.first_appointments.new.heading" %> <%= participant.first_name %> <%= participant.last_name %></h1>

<div class="row well">
  <div class="col-md-10">
    <%= render 'form', {first_appointment: @first_appointment, nurse_participant_evaluation: @nurse_participant_evaluation} %>
  </div>
</div>