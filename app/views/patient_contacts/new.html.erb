<h1><%=t "conemo.views.first_contacts.new.heading" %>  for <%=participant.first_name%><%=participant.last_name%></h1>

<div class="row well">
  <div class="col-md-10">
    <%= render 'form', patient_contact: @patient_contact %>
  </div>
</div>