<h1><%= t "conemo.views.second_contacts.new.heading" %>
  for <%= participant.first_name %> <%= participant.last_name %></h1>

<div class="row well">
  <div class="col-md-10">
    <%= render 'form', second_contact: @second_contact %>
  </div>
</div>