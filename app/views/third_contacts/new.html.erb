<h1><%= t "conemo.views.third_contacts.new.heading" %> <%= participant.first_name %> <%= participant.last_name %></h1>

<div class="row well">
  <div class="col-md-10">
    <%= render 'form', third_contact: @third_contact %>
  </div>
</div>