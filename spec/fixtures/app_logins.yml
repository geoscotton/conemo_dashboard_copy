activelog1:
  occurred_at: <%= DateTime.current - 1.day %>
  participant: active_participant

activelog2:
  occurred_at: <%= DateTime.current - 18.hours %>
  participant: active_participant

activelog3:
  occurred_at: <%= DateTime.current - 12.hours %>
  participant: active_participant

activelog3:
  occurred_at: <%= DateTime.current %>
  participant: active_participant