participant1:
  participant: participant1
  date: <%= Time.use_zone(Participant::TIMEZONES["en"]) { Time.zone.today } %>
  uuid: <%= SecureRandom.uuid %>

active_participant:
  participant: active_participant
  date: <%= Time.use_zone(Participant::TIMEZONES["en"]) { Time.zone.today } %>
  uuid: <%= SecureRandom.uuid %>

active_participant_day_5:
  participant: active_participant_day_5
  date: <%= Time.use_zone(Participant::TIMEZONES["en"]) { Time.zone.today - 4.days } %>
  uuid: <%= SecureRandom.uuid %>

portuguese_active_participant:
  participant: portuguese_active_participant
  date: <%= Time.zone.today %>
  uuid: <%= SecureRandom.uuid %>

portuguese_active_participant2:
  participant: portuguese_active_participant2
  date: <%= Time.zone.today %>
  uuid: <%= SecureRandom.uuid %>

es_active_participant:
  participant: es_active_participant
  date: <%= Time.zone.today %>
  uuid: <%= SecureRandom.uuid %>

english_active_participant:
  participant: english_active_participant
  date: <%= Time.use_zone(Participant::TIMEZONES["en"]) { Time.zone.today } %>
  uuid: <%= SecureRandom.uuid %>

english_inactive_participant:
  participant: english_inactive_participant
  date: <%= Time.use_zone(Participant::TIMEZONES["en"]) { Time.zone.today } %>
  uuid: <%= SecureRandom.uuid %>

es_terminated_participant:
  participant: es_terminated_participant
  date: <%= Time.zone.today %>
  uuid: <%= SecureRandom.uuid %>
