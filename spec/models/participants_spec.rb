require "spec_helper"

describe Participant do
  it "gets the current lesson"
  it "gets the previous lesson"
  it "sees if the current lesson was missed"
  it "sees if the previous lesson was missed"
  it "returns the current participant study status"
end