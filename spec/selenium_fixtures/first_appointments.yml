<% (320..328).each do |pt_id| %>
  first_appointment_<%= pt_id %>:
    participant: participant<%= pt_id %>
    appointment_at: <%= DateTime.now - 7 %>
    appointment_location: Test Location
    session_length: 120
    next_contact: <%= DateTime.now %>
    notes: So many notes
<% end %>

<% (329..336).each do |pt_id| %>
  first_appointment_<%= pt_id %>:
    participant: participant<%= pt_id %>
    appointment_at: <%= DateTime.now - 28 %>
    appointment_location: Test Location
    session_length: 120
    next_contact: <%= DateTime.now - 21 %>
    notes: So many notes
<% end %>

<% (337..343).each do |pt_id| %>
  first_appointment_<%= pt_id %>:
    participant: participant<%= pt_id %>
    appointment_at: <%= DateTime.now - 42 %>
    appointment_location: Test Location
    session_length: 120
    next_contact: <%= DateTime.now - 35 %>
    notes: So many notes
<% end %>

<% (700..705).each do |pt_id| %>
  first_appointment_<%= pt_id %>:
    participant: participant<%= pt_id %>
    appointment_at: <%= DateTime.now - 42 %>
    appointment_location: Test Location
    session_length: 120
    next_contact: <%= DateTime.now - 35 %>
    notes: So many notes
<% end %>
