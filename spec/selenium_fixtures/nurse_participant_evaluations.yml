<% (329..336).each do |pt_id| %>
  nurse_participant_evaluation_<%= pt_id %>:
    second_contact: second_contact_<%= pt_id %>
    q1: 1 - Seems to have great difficulty using the application
    q2: 1 – Not interested
    q3: 1 – Not probable
<% end %>

<% (337..343).each do |pt_id| %>
  nurse_participant_evaluation_<%= pt_id %>:
    second_contact: second_contact_<%= pt_id %>
    q1: 1 - Seems to have great difficulty using the application
    q2: 1 – Not interested
    q3: 1 – Not probable
    third_contact: third_contact_<%= pt_id %>
<% end %>
