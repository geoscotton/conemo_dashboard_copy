<% (301..302).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (303..303).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 1.hours %>
    overdue_at: <%= DateTime.now + 3.days - 1.hours %>
<% end %>

<% (304..310).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (311..319).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 3.days %>
    overdue_at: <%= DateTime.now %>
<% end %>

<% (320..328).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 10.days %>
    overdue_at: <%= DateTime.now - 7.days %>
<% end %>

<% (329..336).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 24.days %>
    overdue_at: <%= DateTime.now + 21.days %>
<% end %>

<% (337..343).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 45.days %>
    overdue_at: <%= DateTime.now - 42.days %>
<% end %>

<% (700..705).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 45.days %>
    overdue_at: <%= DateTime.now - 42.days %>
<% end %>

<% (706..706).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 47.days %>
    overdue_at: <%= DateTime.now - 42.days %>
<% end %>

<% (707..709).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 45.days %>
    overdue_at: <%= DateTime.now - 42.days %>
<% end %>

<% (800..801).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 49.days %>
    overdue_at: <%= DateTime.now - 46.days %>
<% end %>

<% (311..318).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (319..319).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (320..328).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 7.days %>
    overdue_at: <%= DateTime.now - 4.days %>
<% end %>

<% (329..336).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 21.days %>
    overdue_at: <%= DateTime.now - 18.days %>
<% end %>

<% (337..343).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 42.days %>
    overdue_at: <%= DateTime.now - 39.days %>
<% end %>

<% (700..705).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 42.days %>
    overdue_at: <%= DateTime.now - 39.days %>
<% end %>

<% (708..709).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 42.days %>
    overdue_at: <%= DateTime.now - 39.days %>
<% end %>

<% (707..707).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now - 42.days %>
    overdue_at: <%= DateTime.now - 39.days %>
<% end %>

<% (800..801).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 46.days %>
    overdue_at: <%= DateTime.now - 43.days %>
<% end %>

<% (320..321).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (322..322).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: active
    scheduled_at: <%= DateTime.now - 2.days - 2.hours %>
    overdue_at: <%= DateTime.now + 1.days - 2.hours %>
<% end %>

<% (323..328).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (329..336).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: resolved
    scheduled_at: <%= DateTime.now - 14.days %>
    overdue_at: <%= DateTime.now - 11.days %>
<% end %>

<% (337..343).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: resolved
    scheduled_at: <%= DateTime.now - 35.days %>
    overdue_at: <%= DateTime.now - 32.days %>
<% end %>

<% (700..705).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: resolved
    scheduled_at: <%= DateTime.now - 35.days %>
    overdue_at: <%= DateTime.now - 32.days %>
<% end %>

<% (709..709).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: resolved
    scheduled_at: <%= DateTime.now - 35.days %>
    overdue_at: <%= DateTime.now - 32.days %>
<% end %>

<% (708..708).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: active
    scheduled_at: <%= DateTime.now - 35.days %>
    overdue_at: <%= DateTime.now - 32.days %>
<% end %>

<% (800..801).each do |pt_id| %>
  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: resolved
    scheduled_at: <%= DateTime.now - 39.days %>
    overdue_at: <%= DateTime.now - 36.days %>
<% end %>

<% (320..328).each do |pt_id| %>
  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: active
    scheduled_at: <%= DateTime.now + 14.days %>
    overdue_at: <%= DateTime.now + 17.days %>
<% end %>

<% (329..336).each do |pt_id| %>
  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (337..343).each do |pt_id| %>
  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: resolved
    scheduled_at: <%= DateTime.now - 14.days %>
    overdue_at: <%= DateTime.now - 11.days %>
<% end %>

<% (700..705).each do |pt_id| %>
  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: resolved
    scheduled_at: <%= DateTime.now - 14.days %>
    overdue_at: <%= DateTime.now - 11.days %>
<% end %>

<% (709..709).each do |pt_id| %>
  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: active
    scheduled_at: <%= DateTime.now - 14.days %>
    overdue_at: <%= DateTime.now - 11.days %>
<% end %>

<% (800..801).each do |pt_id| %>
  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: resolved
    scheduled_at: <%= DateTime.now - 18.days %>
    overdue_at: <%= DateTime.now - 15.days %>
<% end %>

<% (320..328).each do |pt_id| %>
  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now + 35.days %>
    overdue_at: <%= DateTime.now + 38.days %>
<% end %>

<% (329..336).each do |pt_id| %>
  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now + 21.days %>
    overdue_at: <%= DateTime.now + 24.days %>
<% end %>

<% (337..343).each do |pt_id| %>
  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: resolved
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (700..701).each do |pt_id| %>
  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (702..702).each do |pt_id| %>
  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (703..705).each do |pt_id| %>
  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (800..800).each do |pt_id| %>
  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now - 4.days - 5.hours %>
    overdue_at: <%= DateTime.now - 1.days - 5.hours %>
<% end %>

<% (801..801).each do |pt_id| %>
  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 4.days %>
    overdue_at: <%= DateTime.now - 1.days %>
<% end %>

<% (337..341).each do |pt_id| %>
  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FinalInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (344..345).each do |pt_id| %>
  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FinalInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (342..343).each do |pt_id| %>
  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FinalInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (400..405).each do |pt_id| %>
  nurse_task_7<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>
<% end %>

<% (410..413).each do |pt_id| %>
  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>
<% end %>

<% (427..427).each do |pt_id| %>
  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>
<% end %>

<% (801..801).each do |pt_id| %>
  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FinalInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now - 4.days - 4.hours %>
    overdue_at: <%= DateTime.now - 1.days - 4.hours %>
<% end %>

<% (801..801).each do |pt_id| %>
  nurse_task_7<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now - 1.days %>
    overdue_at: <%= DateTime.now + 1.days %>
<% end %>

<% (802..802).each do |pt_id| %>
  nurse_task_7<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now - 3.days - 2.hours %>
    overdue_at: <%= DateTime.now - 1.days - 2.hours %>
<% end %>

<% (803..803).each do |pt_id| %>
  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: active
    scheduled_at: <%= DateTime.now - 3.days - 1.hours %>
    overdue_at: <%= DateTime.now - 1.days - 1.hours %>
<% end %>

<% (414..414).each do |pt_id| %>
  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: resolved
    scheduled_at: <%= DateTime.now - 13.hours %>
    overdue_at: <%= DateTime.now + 1.days - 1.hours %>
    created_at: <%= DateTime.now - 13.hours %>
    updated_at: <%= DateTime.now - 13.hours %>
<% end %>

<% (415..415).each do |pt_id| %>
  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: resolved
    scheduled_at: <%= DateTime.now - 11.hours %>
    overdue_at: <%= DateTime.now + 1.days - 1.hours %>
    created_at: <%= DateTime.now - 11.hours %>
    updated_at: <%= DateTime.now - 11.hours %>
<% end %>

<% (804..804).each do |pt_id| %>
  nurse_task_9<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::NonAdherenceCall
    status: active
    scheduled_at: <%= DateTime.now - 3.days %>
    overdue_at: <%= DateTime.now - 1.days %>
<% end %>

<% (706..706).each do |pt_id| %>
  nurse_task_7<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now - 4.days %>
    overdue_at: <%= DateTime.now - 2.days %>
<% end %>

<% (423..424).each do |pt_id| %>
  nurse_task_9<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::NonAdherenceCall
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>
<% end %>

<% (425..425).each do |pt_id| %>
  nurse_task_9<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::NonAdherenceCall
    status: resolved
    scheduled_at: <%= DateTime.now - 2.days %>
    overdue_at: <%= DateTime.now %>
    created_at: <%= DateTime.now - 2.days %>
    updated_at: <%= DateTime.now - 2.days %>
<% end %>

<% (1001..1001).each do |pt_id| %>
  nurse_task_9<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::NonAdherenceCall
    status: resolved
    scheduled_at: <%= DateTime.now - 1.hours %>
    overdue_at: <%= DateTime.now + 2.days %>
    created_at: <%= DateTime.now - 1.hours %>
    updated_at: <%= DateTime.now - 1.hours %>
<% end %>

<% (1000..1000).each do |pt_id| %>
  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: active
    scheduled_at: <%= DateTime.now - 1.days %>
    overdue_at: <%= DateTime.now + 1.days %>

  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 3.days %>
    overdue_at: <%= DateTime.now + 13.hours %>

  nurse_task_7<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now - 2.days %>
    overdue_at: <%= DateTime.now + 13.hours %>
<% end %>

<% (501..501).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 2.hours%>
    overdue_at: <%= DateTime.now + 3.days - 2.hours %>

  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FinalInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_7<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>

  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>
<% end %>

<% (504..504).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 2.hours %>
    overdue_at: <%= DateTime.now + 3.days - 2.hours %>
<% end %>

<% (511..511).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: cancelled
    scheduled_at: <%= DateTime.now  - 28.days %>
    overdue_at: <%= DateTime.now  - 25.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 10.days %>
<% end %>

<% (512..512).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: cancelled
    scheduled_at: <%= DateTime.now  - 28.days %>
    overdue_at: <%= DateTime.now  - 25.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 8.days %>
<% end %>

<% (513..513).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: cancelled
    scheduled_at: <%= DateTime.now  - 28.days %>
    overdue_at: <%= DateTime.now  - 25.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 20.days %>
<% end %>

<% (514..514).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 11.days %>
    overdue_at: <%= DateTime.now  - 14.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 11.days %>
<% end %>

<% (515..515).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 4.days %>
    overdue_at: <%= DateTime.now  - 7.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 4.days %>
<% end %>

<% (516..516).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 16.days %>
    overdue_at: <%= DateTime.now  - 19.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 16.days %>
<% end %>

<% [500, 517, 600, 617].each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 45 %>
    overdue_at: <%= DateTime.now  - 42 %>

  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: resolved
    scheduled_at: <%= DateTime.now  - 42 %>
    overdue_at: <%= DateTime.now  - 39 %>

  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: resolved
    scheduled_at: <%= DateTime.now  - 35 %>
    overdue_at: <%= DateTime.now  - 32 %>

  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: resolved
    scheduled_at: <%= DateTime.now - 14 %>
    overdue_at: <%= DateTime.now  - 11 %>

  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: resolved
    scheduled_at: <%= DateTime.now - 7 %>
    overdue_at: <%= DateTime.now - 4 %>

  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FinalInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>
<% end %>

<% (601..601).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 2.hours %>
    overdue_at: <%= DateTime.now + 3.days - 2.hours %>

  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_3<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekOne
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_4<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FollowUpCallWeekThree
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_5<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::CallToScheduleFinalAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_6<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::FinalInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_7<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>

  nurse_task_8<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: active
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 2.days %>
<% end %>

<% (604..604).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 2.hours %>
    overdue_at: <%= DateTime.now + 3.days - 2.hours %>
<% end %>

<% (611..611).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: cancelled
    scheduled_at: <%= DateTime.now  - 28.days %>
    overdue_at: <%= DateTime.now  - 25.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 10.days %>
<% end %>

<% (612..612).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: cancelled
    scheduled_at: <%= DateTime.now  - 28.days %>
    overdue_at: <%= DateTime.now  - 25.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 8.days %>
<% end %>

<% (613..613).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: cancelled
    scheduled_at: <%= DateTime.now  - 28.days %>
    overdue_at: <%= DateTime.now  - 25.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 20.days %>
<% end %>

<% (614..614).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 11.days %>
    overdue_at: <%= DateTime.now  - 14.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 11.days %>
<% end %>

<% (615..615).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 4.days %>
    overdue_at: <%= DateTime.now  - 7.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 4.days %>
<% end %>

<% (616..616).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now  - 16.days %>
    overdue_at: <%= DateTime.now  - 19.days %>
    created_at: <%= DateTime.now - 28.days %>
    updated_at: <%= DateTime.now - 16.days %>
<% end %>

<% (4000..4010).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now %>
    overdue_at: <%= DateTime.now + 3.days %>

  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::InitialInPersonAppointment
    status: active
    scheduled_at: <%= DateTime.now + 2.days %>
    overdue_at: <%= DateTime.now + 5.days %>
<% end %>

<% (4011..4020).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 4.days %>
    overdue_at: <%= DateTime.now - 1.days %>
<% end %>

<% (4021..4030).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 2.days %>
    overdue_at: <%= DateTime.now + 1.days %>
<% end %>

<% (4024..4024).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::HelpRequest
    status: active
    scheduled_at: <%= DateTime.now - 1.days %>
    overdue_at: <%= DateTime.now + 1.days %>
<% end %>

<% (4025..4025).each do |pt_id| %>
  nurse_task_2<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::LackOfConnectivityCall
    status: active
    scheduled_at: <%= DateTime.now - 1.days %>
    overdue_at: <%= DateTime.now + 1.days %>
<% end %>

<% (4026..4026).each do |pt_id| %>
  nurse_task_9<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::NonAdherenceCall
    status: active
    scheduled_at: <%= DateTime.now - 1.days %>
    overdue_at: <%= DateTime.now + 1.days %>
<% end %>

<% (4063..4067).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 2.days %>
    overdue_at: <%= DateTime.now + 1.days %>
<% end %>

<% (4053..4057).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: active
    scheduled_at: <%= DateTime.now - 1.days %>
    overdue_at: <%= DateTime.now + 2.days %>
<% end %>

<% (4058..4062).each do |pt_id| %>
  nurse_task_1<%= pt_id %>:
    participant: participant<%= pt_id %>
    type: Tasks::ConfirmationCall
    status: resolved
    scheduled_at: <%= DateTime.now - 4.days %>
    overdue_at: <%= DateTime.now - 1.days %>
<% end %>
