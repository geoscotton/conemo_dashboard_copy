<% (329..336).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 21 %>
    session_length: 60
    notes: More notes. No fun
    next_contact: <%= DateTime.now %>
<% end %>

<% (337..343).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    next_contact: <%= DateTime.now - 14 %>
<% end %>

<% (700..705).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    next_contact: <%= DateTime.now - 14 %>
<% end %>

<% (500..500).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    next_contact: <%= DateTime.now - 14 %>
<% end %>

<% (600..600).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    next_contact: <%= DateTime.now - 14 %>
<% end %>
