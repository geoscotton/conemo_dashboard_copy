<% (329..336).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 21 %>
    session_length: 60
    notes: More notes. No fun
    difficulties:
      - Difficulty accessing CONEMO app
<% end %>

<% (337..343).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    difficulties:
      - Difficulty accessing CONEMO app
<% end %>

<% (700..705).each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    difficulties:
      - Difficulty accessing CONEMO app
<% end %>

<% [500, 517].each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    difficulties:
      - Difficulty accessing CONEMO app
<% end %>

<% [600, 617].each do |pt_id| %>
  second_contact_<%= pt_id %>:
    participant: participant<%= pt_id %>
    contact_at: <%= DateTime.now - 35 %>
    session_length: 60
    notes: More notes. No fun
    difficulties:
      - Difficulty accessing CONEMO app
<% end %>
